@model IEnumerable<SystemHotel.Cities>

<select id="city" name="City">
    @foreach (var item in Model)
    {
        if (item is SystemHotel.Cities)
        {
            <option value="@item.CityId">@item.CityName</option>
        }
    }
</select>
